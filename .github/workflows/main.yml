name: Bash Script
on:
  workflow_dispatch:

jobs:
  bash-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Bash script
        run: bash test.sh
  test-script:
    needs: [bash-script]
    runs-on: ubuntu-latest
    #environment:
      #name: DEV

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Bash script
        run: bash test1.sh
  A-script:
    needs: [bash-script]
    runs-on: ubuntu-latest
    #environment:
      #name: DEV

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Bash script
        run: bash test1.sh
  B-script:
    needs: [bash-script]
    runs-on: ubuntu-latest
    environment:
      name: DEV

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Bash script
        run: bash test1.sh         
